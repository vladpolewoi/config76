# Copy this file to secrets.env and fill in real values.
# secrets.env is gitignored — never commit it.

# GitHub PAT with read:packages scope — needed for private npm registry
NODE_AUTH_TOKEN=<your-github-pat>   # github.com/settings/tokens → classic → read:packages
